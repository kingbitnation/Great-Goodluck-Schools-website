/** SchoolPilot platform identity — use for IDs, emails, and internal prefixes. */
const PLATFORM_NAME = 'SchoolPilot'
const PLATFORM_PREFIX = 'SP'
const DOMAIN_VERIFY_PREFIX = 'sp-verify'

module.exports = { PLATFORM_NAME, PLATFORM_PREFIX, DOMAIN_VERIFY_PREFIX }
